const n='---\ntitle: 算法学习笔记（六）：动态规划\ndate: 2023-01-06\ntag: 算法;动态规划\ntop: 0\noriginal: 1\n---\n## 基本概念\n\n动态规划（Dynamic Programming，DP）是一种通过将原问题分解为**子问题**并**存储子问题结果**来避免重复计算的算法设计方法。它的核心思想是：\n\n1. **将原问题分解为若干子问题**：子问题与原问题具有相同的结构\n2. **求解子问题**：从最小的子问题开始，逐步求解更大的子问题\n3. **存储子问题的解**：避免重复计算\n4. **利用子问题的解求解原问题**：通过组合子问题的解得到原问题的解\n\n## 适用条件\n\n使用动态规划的前提条件：\n\n1. **重叠子问题**：原问题可以分解为多个相同的子问题，这些子问题会被重复计算\n2. **最优子结构**：原问题的最优解可以通过子问题的最优解组合得到\n\n## 实现步骤\n\n1. **定义状态**：确定如何表示子问题\n2. **确定状态转移方程**：描述子问题之间的关系\n3. **初始化边界条件**：确定最小子问题的解\n4. **计算状态**：按照一定的顺序计算所有状态\n5. **构造最优解**：根据计算结果构造原问题的最优解\n\n## 经典问题\n\n### 1. 斐波那契数列\n\n**问题**：计算第 n 个斐波那契数\n\n**状态定义**：`dp[i]` 表示第 i 个斐波那契数\n\n**状态转移方程**：`dp[i] = dp[i-1] + dp[i-2]`\n\n**边界条件**：`dp[0] = 0, dp[1] = 1`\n\n**实现代码**：\n\n```cpp\n#include <iostream>\n#include <vector>\n\nint fibonacci(int n) {\n    if (n <= 1) return n;\n    \n    std::vector<int> dp(n + 1);\n    dp[0] = 0;\n    dp[1] = 1;\n    \n    for (int i = 2; i <= n; i++) {\n        dp[i] = dp[i-1] + dp[i-2];\n    }\n    \n    return dp[n];\n}\n\n// 空间优化版本\nint fibonacciOptimized(int n) {\n    if (n <= 1) return n;\n    \n    int prev = 0;\n    int curr = 1;\n    \n    for (int i = 2; i <= n; i++) {\n        int next = prev + curr;\n        prev = curr;\n        curr = next;\n    }\n    \n    return curr;\n}\n\nint main() {\n    int n = 10;\n    std::cout << "斐波那契数列第 " << n << " 项: " << fibonacci(n) << std::endl;\n    std::cout << "空间优化版本: " << fibonacciOptimized(n) << std::endl;\n    return 0;\n}\n```\n\n### 2. 爬楼梯\n\n**问题**：假设你正在爬楼梯。需要 n 阶你才能到达楼顶。每次你可以爬 1 或 2 个台阶。你有多少种不同的方法可以爬到楼顶？\n\n**状态定义**：`dp[i]` 表示爬到第 i 阶的方法数\n\n**状态转移方程**：`dp[i] = dp[i-1] + dp[i-2]`\n\n**边界条件**：`dp[1] = 1, dp[2] = 2`\n\n**实现代码**：\n\n```cpp\n#include <iostream>\n#include <vector>\n\nint climbStairs(int n) {\n    if (n <= 2) return n;\n    \n    std::vector<int> dp(n + 1);\n    dp[1] = 1;\n    dp[2] = 2;\n    \n    for (int i = 3; i <= n; i++) {\n        dp[i] = dp[i-1] + dp[i-2];\n    }\n    \n    return dp[n];\n}\n\n// 空间优化版本\nint climbStairsOptimized(int n) {\n    if (n <= 2) return n;\n    \n    int prev1 = 1; // dp[i-2]\n    int prev2 = 2; // dp[i-1]\n    \n    for (int i = 3; i <= n; i++) {\n        int current = prev1 + prev2;\n        prev1 = prev2;\n        prev2 = current;\n    }\n    \n    return prev2;\n}\n\nint main() {\n    int n = 5;\n    std::cout << "爬到第 " << n << " 阶的方法数: " << climbStairs(n) << std::endl;\n    std::cout << "空间优化版本: " << climbStairsOptimized(n) << std::endl;\n    return 0;\n}\n```\n\n### 3. 最大子数组和\n\n**问题**：给定一个整数数组 nums ，找到一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。\n\n**状态定义**：`dp[i]` 表示以第 i 个元素结尾的最大子数组和\n\n**状态转移方程**：`dp[i] = max(nums[i], dp[i-1] + nums[i])`\n\n**边界条件**：`dp[0] = nums[0]`\n\n**实现代码**：\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint maxSubArray(std::vector<int>& nums) {\n    int n = nums.size();\n    std::vector<int> dp(n);\n    dp[0] = nums[0];\n    int maxSum = dp[0];\n    \n    for (int i = 1; i < n; i++) {\n        dp[i] = std::max(nums[i], dp[i-1] + nums[i]);\n        maxSum = std::max(maxSum, dp[i]);\n    }\n    \n    return maxSum;\n}\n\n// 空间优化版本\nint maxSubArrayOptimized(std::vector<int>& nums) {\n    int currentSum = nums[0];\n    int maxSum = nums[0];\n    \n    for (int i = 1; i < nums.size(); i++) {\n        currentSum = std::max(nums[i], currentSum + nums[i]);\n        maxSum = std::max(maxSum, currentSum);\n    }\n    \n    return maxSum;\n}\n\nint main() {\n    std::vector<int> nums = {-2, 1, -3, 4, -1, 2, 1, -5, 4};\n    std::cout << "最大子数组和: " << maxSubArray(nums) << std::endl;\n    std::cout << "空间优化版本: " << maxSubArrayOptimized(nums) << std::endl;\n    return 0;\n}\n```\n\n### 4. 最长递增子序列\n\n**问题**：给你一个整数数组 nums ，找到其中最长严格递增子序列的长度。\n\n**状态定义**：`dp[i]` 表示以第 i 个元素结尾的最长递增子序列的长度\n\n**状态转移方程**：`dp[i] = max(dp[j] + 1) for j < i and nums[j] < nums[i]`\n\n**边界条件**：`dp[i] = 1` 对所有 i\n\n**实现代码**：\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint lengthOfLIS(std::vector<int>& nums) {\n    int n = nums.size();\n    std::vector<int> dp(n, 1);\n    int maxLength = 1;\n    \n    for (int i = 1; i < n; i++) {\n        for (int j = 0; j < i; j++) {\n            if (nums[j] < nums[i]) {\n                dp[i] = std::max(dp[i], dp[j] + 1);\n            }\n        }\n        maxLength = std::max(maxLength, dp[i]);\n    }\n    \n    return maxLength;\n}\n\nint main() {\n    std::vector<int> nums = {10, 9, 2, 5, 3, 7, 101, 18};\n    std::cout << "最长递增子序列长度: " << lengthOfLIS(nums) << std::endl;\n    return 0;\n}\n```\n\n### 5. 0-1 背包问题\n\n**问题**：给定 n 个物品，每个物品有重量 w_i 和价值 v_i，以及一个容量为 C 的背包。选择哪些物品放入背包，使得总重量不超过 C 的情况下，总价值最大。\n\n**状态定义**：`dp[i][j]` 表示前 i 个物品，背包容量为 j 时的最大价值\n\n**状态转移方程**：\n- 不选第 i 个物品：`dp[i][j] = dp[i-1][j]`\n- 选第 i 个物品：`dp[i][j] = dp[i-1][j-w[i]] + v[i]`\n- 取最大值：`dp[i][j] = max(dp[i-1][j], dp[i-1][j-w[i]] + v[i])`\n\n**边界条件**：`dp[0][j] = 0, dp[i][0] = 0`\n\n**实现代码**：\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <algorithm>\n\nint knapsack(std::vector<int>& weights, std::vector<int>& values, int capacity) {\n    int n = weights.size();\n    std::vector<std::vector<int>> dp(n + 1, std::vector<int>(capacity + 1, 0));\n    \n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= capacity; j++) {\n            if (weights[i-1] <= j) {\n                // 可以选择放入或不放入\n                dp[i][j] = std::max(\n                    dp[i-1][j],\n                    dp[i-1][j - weights[i-1]] + values[i-1]\n                );\n            } else {\n                // 无法放入，只能选择不放入\n                dp[i][j] = dp[i-1][j];\n            }\n        }\n    }\n    \n    return dp[n][capacity];\n}\n\n// 空间优化版本\nint knapsackOptimized(std::vector<int>& weights, std::vector<int>& values, int capacity) {\n    int n = weights.size();\n    std::vector<int> dp(capacity + 1, 0);\n    \n    for (int i = 0; i < n; i++) {\n        // 逆序遍历，避免重复使用同一物品\n        for (int j = capacity; j >= weights[i]; j--) {\n            dp[j] = std::max(dp[j], dp[j - weights[i]] + values[i]);\n        }\n    }\n    \n    return dp[capacity];\n}\n\nint main() {\n    std::vector<int> weights = {1, 2, 3, 4};\n    std::vector<int> values = {10, 20, 30, 40};\n    int capacity = 5;\n    \n    std::cout << "0-1 背包最大价值: " << knapsack(weights, values, capacity) << std::endl;\n    std::cout << "空间优化版本: " << knapsackOptimized(weights, values, capacity) << std::endl;\n    return 0;\n}\n```\n\n## 时间复杂度分析\n\n动态规划的时间复杂度取决于问题的规模和状态转移的复杂度：\n\n- **状态数量**：通常为 O(n)、O(n²) 或 O(n³) 等\n- **状态转移复杂度**：每个状态的计算时间\n- **总时间复杂度**：状态数量 × 状态转移复杂度\n\n例如：\n- 斐波那契数列：O(n)\n- 爬楼梯：O(n)\n- 最大子数组和：O(n)\n- 最长递增子序列：O(n²)\n- 0-1 背包问题：O(nC)，其中 C 是背包容量\n\n## 空间复杂度分析\n\n动态规划的空间复杂度取决于状态的存储方式：\n\n- **原始版本**：通常为 O(n)、O(n²) 或 O(nC) 等\n- **空间优化版本**：通常可以将空间复杂度降低到 O(1) 或 O(n)\n\n## 优缺点\n\n### 优点\n\n1. **避免重复计算**：通过存储子问题的解，避免了重复计算，提高了效率\n2. **全局最优解**：可以找到全局最优解，而不是局部最优解\n3. **适用范围广**：可以解决各种优化问题，如组合优化、序列优化等\n4. **实现相对简单**：一旦状态定义和状态转移方程确定，实现相对简单\n\n### 缺点\n\n1. **空间复杂度**：对于某些问题，需要较大的存储空间\n2. **状态定义困难**：对于复杂问题，状态定义和状态转移方程可能难以确定\n3. **时间复杂度**：对于某些问题，时间复杂度可能较高\n4. **不适合所有问题**：只适用于具有重叠子问题和最优子结构的问题\n\n## 动态规划与贪心算法的比较\n\n| 特性 | 动态规划 | 贪心算法 |\n|------|---------|----------|\n| 决策时机 | 自底向上，先求解子问题，再做决策 | 自顶向下，每一步都做局部最优决策 |\n| 最优性 | 保证找到全局最优解 | 只保证找到局部最优解，不一定是全局最优 |\n| 适用条件 | 重叠子问题和最优子结构 | 贪心选择性质和最优子结构 |\n| 时间复杂度 | 通常较高 | 通常较低 |\n| 空间复杂度 | 通常较高 | 通常较低 |\n| 适用场景 | 最长递增子序列、背包问题等 | 活动选择、霍夫曼编码等 |\n\n## 总结\n\n动态规划是一种非常强大的算法设计方法，它通过将原问题分解为子问题并存储子问题的解来避免重复计算，从而提高算法的效率。动态规划适用于具有重叠子问题和最优子结构的问题，如斐波那契数列、爬楼梯、最大子数组和、最长递增子序列、背包问题等。\n\n掌握动态规划算法对于解决各种优化问题非常有帮助，它也是许多高级算法的基础。在实际应用中，我们需要根据具体问题的特点，选择合适的算法设计方法，并结合优化技巧来提高算法的效率。';export{n as default};
