const n="---\ntitle: 算法学习笔记（五）：深度优先搜索\ndate: 2023-01-05\ntag: 算法;深度优先搜索\ntop: 0  \noriginal: 1\n---\n## 基本概念\n\n深度优先搜索（Depth-First Search，DFS）是一种用于遍历或搜索树或图的算法。它的基本思想是：\n\n1. 从根节点（或起始节点）开始\n2. 沿着一条路径尽可能深地搜索，直到不能继续为止\n3. 然后回溯到上一个节点，继续搜索其他路径\n4. 以此类推，直到找到目标节点或遍历完所有节点\n\nDFS 算法通常使用**栈**数据结构来实现，因为栈具有后进先出（LIFO）的特性，适合回溯操作。DFS 也可以使用递归实现，因为递归本身就是一种栈结构。\n\n## 实现步骤\n\n1. **初始化**：创建一个栈，并将起始节点加入栈\n2. **标记访问**：标记起始节点为已访问\n3. **循环处理**：\n   - 从栈中弹出一个节点\n   - 访问该节点\n   - 找到该节点的所有未访问邻居节点\n   - 将这些邻居节点加入栈\n   - 标记这些邻居节点为已访问\n4. **终止条件**：栈为空（遍历完所有节点）或找到目标节点\n\n## 实现代码\n\n### 递归实现\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <unordered_set>\n#include <unordered_map>\n\nvoid dfsRecursive(const std::unordered_map<char, std::vector<char>>& graph, char startNode, std::unordered_set<char>& visited) {\n    // 标记当前节点为已访问\n    visited.insert(startNode);\n    \n    // 访问当前节点\n    std::cout << \"访问节点: \" << startNode << std::endl;\n    \n    // 遍历所有邻居节点\n    auto it = graph.find(startNode);\n    if (it != graph.end()) {\n        for (char neighbor : it->second) {\n            if (visited.find(neighbor) == visited.end()) {\n                // 递归访问邻居节点\n                dfsRecursive(graph, neighbor, visited);\n            }\n        }\n    }\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> graph = {\n        {'A', {'B', 'C'}},\n        {'B', {'A', 'D', 'E'}},\n        {'C', {'A', 'F'}},\n        {'D', {'B'}},\n        {'E', {'B', 'F'}},\n        {'F', {'C', 'E'}}\n    };\n    \n    std::unordered_set<char> visited;\n    // 从节点 A 开始 DFS\n    dfsRecursive(graph, 'A', visited);\n    // 输出: 访问节点: A, 访问节点: B, 访问节点: D, 访问节点: E, 访问节点: F, 访问节点: C\n    \n    return 0;\n}\n```\n\n### 迭代实现\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <stack>\n#include <unordered_set>\n#include <unordered_map>\n#include <algorithm>\n\nvoid dfsIterative(const std::unordered_map<char, std::vector<char>>& graph, char startNode) {\n    std::unordered_set<char> visited; // 用于标记已访问的节点\n    std::stack<char> stack; // 用于存储待访问的节点\n    \n    // 初始化\n    stack.push(startNode);\n    visited.insert(startNode);\n    \n    while (!stack.empty()) {\n        // 从栈中弹出一个节点\n        char currentNode = stack.top();\n        stack.pop();\n        \n        // 访问该节点\n        std::cout << \"访问节点: \" << currentNode << std::endl;\n        \n        // 找到该节点的所有未访问邻居节点\n        // 注意：为了保持与递归实现相同的访问顺序，需要逆序遍历邻居节点\n        auto it = graph.find(currentNode);\n        if (it != graph.end()) {\n            std::vector<char> neighbors = it->second;\n            std::reverse(neighbors.begin(), neighbors.end());\n            \n            for (char neighbor : neighbors) {\n                if (visited.find(neighbor) == visited.end()) {\n                    // 将邻居节点加入栈\n                    stack.push(neighbor);\n                    // 标记为已访问\n                    visited.insert(neighbor);\n                }\n            }\n        }\n    }\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> graph = {\n        {'A', {'B', 'C'}},\n        {'B', {'A', 'D', 'E'}},\n        {'C', {'A', 'F'}},\n        {'D', {'B'}},\n        {'E', {'B', 'F'}},\n        {'F', {'C', 'E'}}\n    };\n    \n    // 从节点 A 开始 DFS\n    dfsIterative(graph, 'A');\n    // 输出: 访问节点: A, 访问节点: B, 访问节点: D, 访问节点: E, 访问节点: F, 访问节点: C\n    \n    return 0;\n}\n```\n\n## 应用场景\n\n### 1. 拓扑排序\n\n**问题**：在有向无环图（DAG）中，找到一个拓扑顺序，使得对于每一条有向边 (u, v)，u 都在 v 的前面\n\n**解法**：使用 DFS 遍历图，当一个节点的所有邻居都被访问完后，将该节点加入结果列表，最后反转列表得到拓扑顺序\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <unordered_set>\n#include <unordered_map>\n#include <algorithm>\n\nbool topologicalSortDFS(const std::unordered_map<char, std::vector<char>>& graph, char node, \n                      std::unordered_set<char>& visited, \n                      std::unordered_set<char>& tempVisited, \n                      std::vector<char>& result) {\n    if (tempVisited.find(node) != tempVisited.end()) {\n        return true; // 存在环\n    }\n    \n    if (visited.find(node) != visited.end()) {\n        return false;\n    }\n    \n    tempVisited.insert(node);\n    visited.insert(node);\n    \n    auto it = graph.find(node);\n    if (it != graph.end()) {\n        for (char neighbor : it->second) {\n            if (topologicalSortDFS(graph, neighbor, visited, tempVisited, result)) {\n                return true;\n            }\n        }\n    }\n    \n    tempVisited.erase(node);\n    result.push_back(node);\n    return false;\n}\n\nstd::vector<char> topologicalSort(const std::unordered_map<char, std::vector<char>>& graph) {\n    std::unordered_set<char> visited;\n    std::unordered_set<char> tempVisited; // 用于检测环\n    std::vector<char> result;\n    bool hasCycle = false;\n    \n    // 遍历所有节点\n    for (const auto& pair : graph) {\n        char node = pair.first;\n        if (visited.find(node) == visited.end()) {\n            if (topologicalSortDFS(graph, node, visited, tempVisited, result)) {\n                hasCycle = true;\n                break;\n            }\n        }\n    }\n    \n    if (hasCycle) {\n        std::cerr << \"图中存在环，无法进行拓扑排序\" << std::endl;\n        return {};\n    }\n    \n    std::reverse(result.begin(), result.end());\n    return result;\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> dag = {\n        {'A', {'B', 'C'}},\n        {'B', {'D'}},\n        {'C', {'D'}},\n        {'D', {'E'}},\n        {'E', {}}\n    };\n    \n    std::vector<char> order = topologicalSort(dag);\n    \n    std::cout << \"拓扑排序结果: \";\n    for (size_t i = 0; i < order.size(); i++) {\n        std::cout << order[i];\n        if (i < order.size() - 1) std::cout << \" -> \";\n    }\n    std::cout << std::endl;\n    // 输出: [ 'A', 'C', 'B', 'D', 'E' ]\n    \n    return 0;\n}\n```\n\n### 2. 寻找所有路径\n\n**问题**：找到从起点到终点的所有路径\n\n**解法**：使用 DFS 遍历图，记录当前路径，当到达终点时保存路径\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <unordered_set>\n#include <unordered_map>\n\nvoid findAllPathsDFS(const std::unordered_map<char, std::vector<char>>& graph, char current, char end, \n                    std::unordered_set<char>& visited, std::vector<char>& path, \n                    std::vector<std::vector<char>>& paths) {\n    // 标记当前节点为已访问\n    visited.insert(current);\n    // 将当前节点加入路径\n    path.push_back(current);\n    \n    if (current == end) {\n        // 找到一条路径，保存副本\n        paths.push_back(path);\n    } else {\n        // 遍历所有邻居节点\n        auto it = graph.find(current);\n        if (it != graph.end()) {\n            for (char neighbor : it->second) {\n                if (visited.find(neighbor) == visited.end()) {\n                    findAllPathsDFS(graph, neighbor, end, visited, path, paths);\n                }\n            }\n        }\n    }\n    \n    // 回溯：移除当前节点从路径和访问集合\n    path.pop_back();\n    visited.erase(current);\n}\n\nstd::vector<std::vector<char>> findAllPaths(const std::unordered_map<char, std::vector<char>>& graph, char start, char end) {\n    std::vector<std::vector<char>> paths;\n    std::unordered_set<char> visited;\n    std::vector<char> path;\n    \n    findAllPathsDFS(graph, start, end, visited, path, paths);\n    return paths;\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> graphWithPaths = {\n        {'A', {'B', 'C'}},\n        {'B', {'D', 'E'}},\n        {'C', {'F'}},\n        {'D', {'F'}},\n        {'E', {'F'}},\n        {'F', {}}\n    };\n    \n    std::vector<std::vector<char>> paths = findAllPaths(graphWithPaths, 'A', 'F');\n    \n    std::cout << \"所有路径: \" << std::endl;\n    for (const auto& path : paths) {\n        for (size_t i = 0; i < path.size(); i++) {\n            std::cout << path[i];\n            if (i < path.size() - 1) std::cout << \" -> \";\n        }\n        std::cout << std::endl;\n    }\n    // 输出: [ [ 'A', 'B', 'D', 'F' ], [ 'A', 'B', 'E', 'F' ], [ 'A', 'C', 'F' ] ]\n    \n    return 0;\n}\n```\n\n### 3. 回溯问题\n\n**问题**：解决需要尝试所有可能解决方案的问题，如八皇后问题、数独问题等\n\n**解法**：使用 DFS 遍历所有可能的解决方案，当遇到无效解时回溯\n\n### 4. 连通性检测\n\n**问题**：检测图中两个节点是否连通，或检测图的连通分量\n\n**解法**：使用 DFS 从一个节点开始遍历，看是否能到达另一个节点\n\n## 时间复杂度分析\n\n- **时间复杂度**：O(V + E)，其中 V 是节点数，E 是边数\n  - 每个节点最多被访问一次，时间复杂度为 O(V)\n  - 每条边最多被访问一次，时间复杂度为 O(E)\n\n- **空间复杂度**：O(V)\n  - 最坏情况下，栈需要存储所有节点，空间复杂度为 O(V)\n  - 访问集合需要存储所有节点，空间复杂度为 O(V)\n\n## 优缺点\n\n### 优点\n\n1. **内存使用**：对于深度较浅的图，DFS 比 BFS 更节省内存，因为它只需要存储一条路径\n2. **适合回溯问题**：DFS 天然适合需要回溯的问题，如八皇后问题、数独问题等\n3. **可以找到所有路径**：DFS 可以找到从起点到终点的所有路径\n4. **实现简单**：递归实现非常简洁易懂\n\n### 缺点\n\n1. **不能找到最短路径**：DFS 不能保证找到最短路径，因为它可能沿着一条长路径一直搜索\n2. **可能陷入无限循环**：对于有环的图，如果不标记已访问节点，可能会陷入无限循环\n3. **递归深度限制**：递归实现可能会遇到栈溢出的问题，对于深度较大的图需要使用迭代实现\n4. **不适合层次遍历**：DFS 不适合需要层次信息的场景\n\n## DFS 与 BFS 的比较\n\n| 特性 | DFS | BFS |\n|------|-----|-----|\n| 数据结构 | 栈（或递归） | 队列 |\n| 遍历顺序 | 深度优先（路径） | 广度优先（层级） |\n| 最短路径 | 不能保证找到最短路径 | 可以找到无权图的最短路径 |\n| 内存使用 | 可能较小（存储一条路径） | 可能较大（存储整层节点） |\n| 适用场景 | 拓扑排序、回溯问题、寻找所有路径 | 最短路径、层次遍历、连通性检测 |\n| 实现方式 | 递归或迭代 | 迭代 |\n| 回溯能力 | 天然支持回溯 | 需要额外处理才能回溯 |\n\n## 优化技巧\n\n1. **剪枝**：在搜索过程中，提前排除不可能的路径，减少搜索空间\n\n2. **记忆化**：对于重复的子问题，使用记忆化技术存储结果，避免重复计算\n\n3. **迭代加深**：结合 BFS 和 DFS 的优点，逐步增加搜索深度\n\n4. **双向 DFS**：同时从起点和终点开始 DFS，当两个搜索相遇时找到路径\n\n### 剪枝示例（八皇后问题）\n\n```cpp\n#include <iostream>\n#include <vector>\n\nbool isSafe(const std::vector<std::string>& board, int row, int col) {\n    int n = board.size();\n    \n    // 检查列\n    for (int i = 0; i < row; i++) {\n        if (board[i][col] == 'Q') {\n            return false;\n        }\n    }\n    \n    // 检查左上对角线\n    for (int i = row - 1, j = col - 1; i >= 0 && j >= 0; i--, j--) {\n        if (board[i][j] == 'Q') {\n            return false;\n        }\n    }\n    \n    // 检查右上对角线\n    for (int i = row - 1, j = col + 1; i >= 0 && j < n; i--, j++) {\n        if (board[i][j] == 'Q') {\n            return false;\n        }\n    }\n    \n    return true;\n}\n\nvoid solveNQueensDFS(int n, int row, std::vector<std::string>& board, std::vector<std::vector<std::string>>& solutions) {\n    if (row == n) {\n        // 找到一个解决方案\n        solutions.push_back(board);\n        return;\n    }\n    \n    for (int col = 0; col < n; col++) {\n        if (isSafe(board, row, col)) {\n            // 放置皇后\n            board[row][col] = 'Q';\n            // 递归到下一行\n            solveNQueensDFS(n, row + 1, board, solutions);\n            // 回溯，移除皇后\n            board[row][col] = '.';\n        }\n    }\n}\n\nstd::vector<std::vector<std::string>> solveNQueens(int n) {\n    std::vector<std::vector<std::string>> solutions;\n    std::vector<std::string> board(n, std::string(n, '.'));\n    \n    solveNQueensDFS(n, 0, board, solutions);\n    return solutions;\n}\n\nvoid printSolutions(const std::vector<std::vector<std::string>>& solutions) {\n    for (size_t i = 0; i < solutions.size(); i++) {\n        std::cout << \"解决方案 \" << i + 1 << \":\" << std::endl;\n        for (const std::string& row : solutions[i]) {\n            std::cout << row << std::endl;\n        }\n        std::cout << std::endl;\n    }\n}\n\nint main() {\n    int n = 4;\n    std::vector<std::vector<std::string>> solutions = solveNQueens(n);\n    printSolutions(solutions);\n    // 输出: [ [ '.Q..', '...Q', 'Q...', '..Q.' ], [ '..Q.', 'Q...', '...Q', '.Q..' ] ]\n    \n    return 0;\n}\n```\n\n## 总结\n\nDFS 是一种非常重要的图遍历算法，它按路径深度搜索节点，适合解决拓扑排序、回溯问题、寻找所有路径等问题。DFS 的核心思想是使用栈（或递归）来存储待访问的节点，确保深度优先搜索和回溯操作。\n\n掌握 DFS 算法对于解决各种图相关的问题非常有帮助，它也是许多高级算法的基础，如 Tarjan 算法、Kruskal 算法等。在实际应用中，我们需要根据具体问题的特点，选择合适的搜索算法，并结合优化技巧来提高算法的效率。";export{n as default};
