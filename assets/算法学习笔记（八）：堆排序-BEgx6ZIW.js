const n="---\ntitle: 算法学习笔记（八）：堆排序\ndate: 2023-01-08\ntag: 算法;堆排序\ntop: 0\noriginal: 1\n---\n## 1. 算法概念\n堆排序（Heap Sort）是一种基于比较的排序算法，它利用堆这种数据结构来进行排序。堆是一种特殊的完全二叉树，分为最大堆和最小堆：\n- **最大堆**：每个节点的值都大于或等于其子节点的值\n- **最小堆**：每个节点的值都小于或等于其子节点的值\n堆排序的基本思想是：\n1. 将待排序序列构建成一个最大堆\n2. 将堆顶元素（最大值）与末尾元素交换\n3. 将剩余n-1个元素重新构建成一个堆\n4. 重复步骤2-3，直到整个序列有序\n\n## 2. 算法实现\n### 2.1 核心函数\n```javascript\n// 构建最大堆\nfunction buildMaxHeap(arr) {\n  // 从最后一个非叶子节点开始向上调整\n  for (let i = Math.floor(arr.length / 2) - 1; i >= 0; i--) {\n    heapify(arr, arr.length, i);\n  }\n}\n\n// 调整堆\nfunction heapify(arr, length, i) {\n  let largest = i; // 初始化largest为根节点\n  const left = 2 * i + 1; // 左子节点\n  const right = 2 * i + 2; // 右子节点\n\n  // 如果左子节点大于根节点\n  if (left < length && arr[left] > arr[largest]) {\n    largest = left;\n  }\n\n  // 如果右子节点大于当前最大值\n  if (right < length && arr[right] > arr[largest]) {\n    largest = right;\n  }\n\n  // 如果最大值不是根节点\n  if (largest !== i) {\n    // 交换根节点与最大值\n    [arr[i], arr[largest]] = [arr[largest], arr[i]];\n    // 递归调整受影响的子堆\n    heapify(arr, length, largest);\n  }\n}\n\n// 堆排序主函数\nfunction heapSort(arr) {\n  const len = arr.length;\n  \n  // 构建最大堆\n  buildMaxHeap(arr);\n  \n  // 一个个从堆顶取出元素\n  for (let i = len - 1; i > 0; i--) {\n    // 将堆顶元素与末尾元素交换\n    [arr[0], arr[i]] = [arr[i], arr[0]];\n    // 对剩余的堆进行调整\n    heapify(arr, i, 0);\n  }\n  \n  return arr;\n}\n```\n\n### 2.2 使用示例\n```javascript\n// 测试堆排序\nconst arr = [12, 11, 13, 5, 6, 7];\nconsole.log('原始数组:', arr);\nconst sortedArr = heapSort(arr);\nconsole.log('排序后数组:', sortedArr);\n\n// 输出结果:\n// 原始数组: [12, 11, 13, 5, 6, 7]\n// 排序后数组: [5, 6, 7, 11, 12, 13]\n```\n\n## 3. 算法分析\n### 3.1 时间复杂度\n- **最好情况**：O(n log n)\n- **最坏情况**：O(n log n)\n- **平均情况**：O(n log n)\n\n### 3.2 空间复杂度\n- **空间复杂度**：O(1)，是原地排序算法\n\n### 3.3 算法特点\n- **稳定性**：不稳定排序\n- **适用场景**：适用于需要找出数组中最大或最小元素的场景，也适用于大规模数据的排序\n- **优势**：时间复杂度稳定，为O(n log n)；空间复杂度低，为O(1)\n- **劣势**：实现相对复杂，对于小规模数据，性能不如插入排序等简单排序算法\n\n## 4. 代码优化\n### 4.1 优化点\n1. **减少交换次数**：在调整堆的过程中，可以先记录最大值的位置，最后只进行一次交换\n2. **使用迭代而非递归**：递归实现的heapify函数可能会导致栈溢出，对于大规模数据，可以使用迭代实现\n3. **提前终止**：如果在调整堆的过程中，没有发生任何交换，说明堆已经是有序的，可以提前终止\n\n### 4.2 优化实现\n```javascript\n// 迭代实现的heapify函数\nfunction heapifyIterative(arr, length, i) {\n  while (true) {\n    let largest = i;\n    const left = 2 * i + 1;\n    const right = 2 * i + 2;\n\n    if (left < length && arr[left] > arr[largest]) {\n      largest = left;\n    }\n\n    if (right < length && arr[right] > arr[largest]) {\n      largest = right;\n    }\n\n    if (largest === i) break;\n\n    [arr[i], arr[largest]] = [arr[largest], arr[i]];\n    i = largest;\n  }\n}\n\n// 优化后的堆排序函数\nfunction optimizedHeapSort(arr) {\n  const len = arr.length;\n  \n  // 构建最大堆\n  for (let i = Math.floor(len / 2) - 1; i >= 0; i--) {\n    heapifyIterative(arr, len, i);\n  }\n  \n  // 一个个从堆顶取出元素\n  for (let i = len - 1; i > 0; i--) {\n    [arr[0], arr[i]] = [arr[i], arr[0]];\n    heapifyIterative(arr, i, 0);\n  }\n  \n  return arr;\n}\n```\n\n## 5. 应用场景\n1. **优先级队列**：堆是实现优先级队列的最佳数据结构\n2. **Top K问题**：例如，找出数组中最大的K个元素\n3. **图算法**：如Dijkstra最短路径算法\n4. **堆排序**：适用于大规模数据的排序\n\n## 6. 总结\n堆排序是一种高效的排序算法，它利用堆这种数据结构来进行排序。堆排序的时间复杂度为O(n log n)，空间复杂度为O(1)，是一种原地排序算法。虽然堆排序的实现相对复杂，但它在处理大规模数据时表现出色，特别是在需要找出最大或最小元素的场景中。\n堆排序的核心思想是：\n1. 将待排序序列构建成一个最大堆\n2. 将堆顶元素与末尾元素交换\n3. 将剩余元素重新构建成一个堆\n4. 重复步骤2-3，直到整个序列有序\n通过理解堆排序的原理和实现，我们可以更好地掌握堆这种数据结构的应用，以及在实际问题中选择合适的排序算法。";export{n as default};
