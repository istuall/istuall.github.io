const n="---\ntitle: 算法学习笔记（四）：广度优先搜索\ndate: 2023-01-04\ntag: 算法;广度优先搜索\ntop: 0  \noriginal: 1\n---\n## 基本概念\n\n广度优先搜索（Breadth-First Search，BFS）是一种用于遍历或搜索树或图的算法。它的基本思想是：\n\n1. 从根节点（或起始节点）开始\n2. 先访问当前层级的所有节点\n3. 然后再访问下一层级的节点\n4. 以此类推，直到找到目标节点或遍历完所有节点\n\nBFS 算法通常使用**队列**数据结构来实现，因为队列具有先进先出（FIFO）的特性，适合按顺序处理节点。\n\n## 实现步骤\n\n1. **初始化**：创建一个队列，并将起始节点加入队列\n2. **标记访问**：标记起始节点为已访问\n3. **循环处理**：\n   - 从队列中取出一个节点\n   - 访问该节点\n   - 找到该节点的所有未访问邻居节点\n   - 将这些邻居节点加入队列\n   - 标记这些邻居节点为已访问\n4. **终止条件**：队列为空（遍历完所有节点）或找到目标节点\n\n## 实现代码\n\n### 图的 BFS 实现\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <unordered_set>\n#include <unordered_map>\n\nvoid bfs(const std::unordered_map<char, std::vector<char>>& graph, char startNode) {\n    std::unordered_set<char> visited; // 用于标记已访问的节点\n    std::queue<char> queue; // 用于存储待访问的节点\n    \n    // 初始化\n    queue.push(startNode);\n    visited.insert(startNode);\n    \n    while (!queue.empty()) {\n        // 从队列中取出一个节点\n        char currentNode = queue.front();\n        queue.pop();\n        \n        // 访问该节点\n        std::cout << \"访问节点: \" << currentNode << std::endl;\n        \n        // 找到该节点的所有未访问邻居节点\n        auto it = graph.find(currentNode);\n        if (it != graph.end()) {\n            for (char neighbor : it->second) {\n                if (visited.find(neighbor) == visited.end()) {\n                    // 将邻居节点加入队列\n                    queue.push(neighbor);\n                    // 标记为已访问\n                    visited.insert(neighbor);\n                }\n            }\n        }\n    }\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> graph = {\n        {'A', {'B', 'C'}},\n        {'B', {'A', 'D', 'E'}},\n        {'C', {'A', 'F'}},\n        {'D', {'B'}},\n        {'E', {'B', 'F'}},\n        {'F', {'C', 'E'}}\n    };\n    \n    // 从节点 A 开始 BFS\n    bfs(graph, 'A');\n    // 输出: 访问节点: A, 访问节点: B, 访问节点: C, 访问节点: D, 访问节点: E, 访问节点: F\n    \n    return 0;\n}\n```\n\n### 二叉树的 BFS 实现\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <queue>\n\n// 二叉树节点定义\nstruct TreeNode {\n    int val;\n    TreeNode* left;\n    TreeNode* right;\n    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}\n};\n\nstd::vector<std::vector<int>> bfsTree(TreeNode* root) {\n    std::vector<std::vector<int>> result;\n    if (!root) return result;\n    \n    std::queue<TreeNode*> queue;\n    queue.push(root);\n    \n    while (!queue.empty()) {\n        int levelSize = queue.size();\n        std::vector<int> currentLevel;\n        \n        // 处理当前层级的所有节点\n        for (int i = 0; i < levelSize; i++) {\n            TreeNode* node = queue.front();\n            queue.pop();\n            currentLevel.push_back(node->val);\n            \n            // 将子节点加入队列\n            if (node->left) queue.push(node->left);\n            if (node->right) queue.push(node->right);\n        }\n        \n        result.push_back(currentLevel);\n    }\n    \n    return result;\n}\n\nint main() {\n    // 示例二叉树\n    TreeNode* root = new TreeNode(1);\n    root->left = new TreeNode(2);\n    root->right = new TreeNode(3);\n    root->left->left = new TreeNode(4);\n    root->left->right = new TreeNode(5);\n    root->right->left = new TreeNode(6);\n    root->right->right = new TreeNode(7);\n    \n    std::vector<std::vector<int>> result = bfsTree(root);\n    \n    // 输出结果\n    std::cout << \"[\";\n    for (size_t i = 0; i < result.size(); i++) {\n        std::cout << \"[\";\n        for (size_t j = 0; j < result[i].size(); j++) {\n            std::cout << result[i][j];\n            if (j < result[i].size() - 1) std::cout << \", \";\n        }\n        std::cout << \"]\";\n        if (i < result.size() - 1) std::cout << \", \";\n    }\n    std::cout << \"]\" << std::endl;\n    // 输出: [[1], [2, 3], [4, 5, 6, 7]]\n    \n    // 释放内存\n    delete root->left->left;\n    delete root->left->right;\n    delete root->left;\n    delete root->right->left;\n    delete root->right->right;\n    delete root->right;\n    delete root;\n    \n    return 0;\n}\n```\n\n## 应用场景\n\n### 1. 最短路径问题\n\n**问题**：在无权图中，找到从起点到终点的最短路径\n\n**解法**：BFS 可以保证找到最短路径，因为它按层级顺序搜索，第一次到达终点时的路径就是最短路径\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <unordered_set>\n#include <unordered_map>\n\nstd::vector<char> shortestPath(const std::unordered_map<char, std::vector<char>>& graph, char start, char end) {\n    std::unordered_set<char> visited;\n    std::queue<std::vector<char>> queue; // 队列中存储的是路径\n    \n    if (start == end) return {start};\n    \n    queue.push({start});\n    \n    while (!queue.empty()) {\n        std::vector<char> path = queue.front();\n        queue.pop();\n        char node = path.back();\n        \n        if (visited.find(node) == visited.end()) {\n            visited.insert(node);\n            \n            auto it = graph.find(node);\n            if (it != graph.end()) {\n                for (char neighbor : it->second) {\n                    std::vector<char> newPath = path;\n                    newPath.push_back(neighbor);\n                    if (neighbor == end) {\n                        return newPath;\n                    }\n                    queue.push(newPath);\n                }\n            }\n        }\n    }\n    \n    return {}; // 没有找到路径\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> graph = {\n        {'A', {'B', 'C'}},\n        {'B', {'A', 'D', 'E'}},\n        {'C', {'A', 'F'}},\n        {'D', {'B'}},\n        {'E', {'B', 'F'}},\n        {'F', {'C', 'E'}}\n    };\n    \n    // 查找最短路径\n    std::vector<char> path = shortestPath(graph, 'A', 'F');\n    \n    // 输出结果\n    std::cout << \"最短路径: \";\n    for (size_t i = 0; i < path.size(); i++) {\n        std::cout << path[i];\n        if (i < path.size() - 1) std::cout << \" -> \";\n    }\n    std::cout << std::endl;\n    // 输出: 最短路径: A -> C -> F 或 A -> B -> E -> F\n    \n    return 0;\n}\n```\n\n### 2. 层次遍历\n\n**问题**：按层级顺序遍历树或图的所有节点\n\n**解法**：BFS 天然适合层次遍历，因为它按层级顺序处理节点\n\n### 3. 连通性检测\n\n**问题**：检测图中两个节点是否连通，或检测图的连通分量\n\n**解法**：使用 BFS 从一个节点开始遍历，看是否能到达另一个节点\n\n### 4. 迷宫求解\n\n**问题**：在迷宫中找到从起点到终点的路径\n\n**解法**：将迷宫视为图，使用 BFS 搜索最短路径\n\n## 时间复杂度分析\n\n- **时间复杂度**：O(V + E)，其中 V 是节点数，E 是边数\n  - 每个节点最多入队和出队一次，时间复杂度为 O(V)\n  - 每条边最多被访问一次，时间复杂度为 O(E)\n\n- **空间复杂度**：O(V)\n  - 最坏情况下，队列需要存储所有节点，空间复杂度为 O(V)\n  - 访问集合需要存储所有节点，空间复杂度为 O(V)\n\n## 优缺点\n\n### 优点\n\n1. **找到最短路径**：在无权图中，BFS 可以保证找到最短路径\n2. **层次遍历**：可以按层级顺序遍历节点，适合需要层次信息的场景\n3. **实现简单**：算法逻辑清晰，易于实现\n4. **内存使用**：在某些情况下，BFS 比深度优先搜索（DFS）更节省内存\n\n### 缺点\n\n1. **内存消耗**：对于大型图，BFS 需要存储大量节点在队列中，内存消耗较大\n2. **不适合深度优先搜索**：对于需要深度优先探索的问题（如迷宫中有很多分支），BFS 可能不是最佳选择\n3. **需要完整的图结构**：BFS 需要知道节点的所有邻居节点，对于动态生成的图可能不适用\n\n## BFS 与 DFS 的比较\n\n| 特性 | BFS | DFS |\n|------|-----|-----|\n| 数据结构 | 队列 | 栈（或递归） |\n| 遍历顺序 | 广度优先（层级） | 深度优先（路径） |\n| 最短路径 | 可以找到无权图的最短路径 | 不能保证找到最短路径 |\n| 内存使用 | 可能较大（存储整层节点） | 可能较小（存储一条路径） |\n| 适用场景 | 最短路径、层次遍历、连通性检测 | 拓扑排序、回溯问题、寻找所有路径 |\n| 实现方式 | 迭代 | 递归或迭代 |\n\n## 优化技巧\n\n1. **双向 BFS**：同时从起点和终点开始 BFS，当两个搜索相遇时找到最短路径，适用于大型图\n\n2. **优先队列**：在加权图中，可以使用优先队列（如 Dijkstra 算法）来找到最短路径\n\n3. **记忆化**：对于重复的状态，使用记忆化技术避免重复处理\n\n4. **剪枝**：在搜索过程中，提前排除不可能的路径，减少搜索空间\n\n### 双向 BFS 实现\n\n```cpp\n#include <iostream>\n#include <vector>\n#include <queue>\n#include <unordered_set>\n#include <unordered_map>\n\nstd::vector<char> bidirectionalBFS(const std::unordered_map<char, std::vector<char>>& graph, char start, char end) {\n    // 从起点开始的搜索\n    std::queue<char> startQueue;\n    std::unordered_set<char> startVisited;\n    std::unordered_map<char, char> startParents;\n    \n    // 从终点开始的搜索\n    std::queue<char> endQueue;\n    std::unordered_set<char> endVisited;\n    std::unordered_map<char, char> endParents;\n    \n    // 初始化\n    startQueue.push(start);\n    startVisited.insert(start);\n    \n    endQueue.push(end);\n    endVisited.insert(end);\n    \n    while (!startQueue.empty() && !endQueue.empty()) {\n        // 处理起点队列\n        char currentStart = startQueue.front();\n        startQueue.pop();\n        \n        if (endVisited.find(currentStart) != endVisited.end()) {\n            // 找到交点，构建路径\n            std::vector<char> path;\n            char current = currentStart;\n            while (startParents.find(current) != startParents.end()) {\n                path.insert(path.begin(), current);\n                current = startParents[current];\n            }\n            path.insert(path.begin(), current);\n            \n            current = endParents[currentStart];\n            while (current != end) {\n                path.push_back(current);\n                current = endParents[current];\n            }\n            path.push_back(current);\n            return path;\n        }\n        \n        auto it = graph.find(currentStart);\n        if (it != graph.end()) {\n            for (char neighbor : it->second) {\n                if (startVisited.find(neighbor) == startVisited.end()) {\n                    startVisited.insert(neighbor);\n                    startQueue.push(neighbor);\n                    startParents[neighbor] = currentStart;\n                }\n            }\n        }\n        \n        // 处理终点队列\n        char currentEnd = endQueue.front();\n        endQueue.pop();\n        \n        if (startVisited.find(currentEnd) != startVisited.end()) {\n            // 找到交点，构建路径\n            std::vector<char> path;\n            char current = currentEnd;\n            while (startParents.find(current) != startParents.end()) {\n                path.insert(path.begin(), current);\n                current = startParents[current];\n            }\n            path.insert(path.begin(), current);\n            \n            current = endParents[currentEnd];\n            while (current != end) {\n                path.push_back(current);\n                current = endParents[current];\n            }\n            path.push_back(current);\n            return path;\n        }\n        \n        it = graph.find(currentEnd);\n        if (it != graph.end()) {\n            for (char neighbor : it->second) {\n                if (endVisited.find(neighbor) == endVisited.end()) {\n                    endVisited.insert(neighbor);\n                    endQueue.push(neighbor);\n                    endParents[neighbor] = currentEnd;\n                }\n            }\n        }\n    }\n    \n    return {}; // 没有找到路径\n}\n\nint main() {\n    // 示例图\n    std::unordered_map<char, std::vector<char>> graph = {\n        {'A', {'B', 'C'}},\n        {'B', {'A', 'D', 'E'}},\n        {'C', {'A', 'F'}},\n        {'D', {'B'}},\n        {'E', {'B', 'F'}},\n        {'F', {'C', 'E'}}\n    };\n    \n    // 使用双向 BFS 查找最短路径\n    std::vector<char> path = bidirectionalBFS(graph, 'A', 'F');\n    \n    // 输出结果\n    std::cout << \"双向 BFS 最短路径: \";\n    for (size_t i = 0; i < path.size(); i++) {\n        std::cout << path[i];\n        if (i < path.size() - 1) std::cout << \" -> \";\n    }\n    std::cout << std::endl;\n    \n    return 0;\n}\n```\n\n## 总结\n\nBFS 是一种非常重要的图遍历算法，它按层级顺序搜索节点，适合解决最短路径、层次遍历、连通性检测等问题。BFS 的核心思想是使用队列来存储待访问的节点，确保按顺序处理每一层级的节点。\n\n掌握 BFS 算法对于解决各种图相关的问题非常有帮助，它也是许多高级算法的基础，如 Dijkstra 算法、Prim 算法等。在实际应用中，我们需要根据具体问题的特点，选择合适的搜索算法，并结合优化技巧来提高算法的效率。";export{n as default};
