const n="---\ntitle: 算法学习笔记（二）：双指针\ndate: 2023-01-02\ntag: 算法;双指针\ntop: 0\noriginal: 1\n---\n## 基本概念\n双指针（Two Pointers）是一种常用的算法技巧，它的基本思想是：\n1. 使用两个指针在数组或链表上移动\n2. 根据问题的要求，调整指针的移动方向和条件\n3. 通过指针的移动来缩小搜索范围或找到问题的解\n双指针算法通常可以将时间复杂度从O(n²)降低到O(n)，是一种非常高效的算法技巧。\n\n## 常见类型\n1. **快慢指针**：一个指针移动速度快，一个指针移动速度慢\n2. **左右指针**：两个指针分别从数组的左右两端开始移动\n3. **滑动窗口**：两个指针形成一个窗口，通过移动窗口来解决问题\n\n## 实现模板\n### 1. 快慢指针\n**应用场景**：查找链表的中点、判断链表是否有环、寻找环的入口等\n```cpp\n// 链表节点定义\nstruct ListNode {\n    int val;\n    ListNode *next;\n    ListNode(int x) : val(x), next(nullptr) {}\n};\n\n// 查找链表的中点\nListNode* findMiddle(ListNode* head) {\n    ListNode* slow = head;\n    ListNode* fast = head;\n    \n    while (fast && fast->next) {\n        slow = slow->next;       // 慢指针每次移动一步\n        fast = fast->next->next;  // 快指针每次移动两步\n    }\n    \n    return slow; // 当链表长度为奇数时，返回中间节点；为偶数时，返回中间偏右的节点\n}\n\n// 判断链表是否有环\nbool hasCycle(ListNode* head) {\n    if (!head || !head->next) return false;\n    \n    ListNode* slow = head;\n    ListNode* fast = head;\n    \n    while (fast && fast->next) {\n        slow = slow->next;\n        fast = fast->next->next;\n        \n        if (slow == fast) {\n            return true; // 有环\n        }\n    }\n    \n    return false; // 无环\n}\n```\n\n### 2. 左右指针\n**应用场景**：二分查找、两数之和、反转数组等\n```cpp\n#include <vector>\n\n// 两数之和（有序数组）\nstd::vector<int> twoSum(std::vector<int>& nums, int target) {\n    int left = 0;\n    int right = nums.size() - 1;\n    \n    while (left < right) {\n        int sum = nums[left] + nums[right];\n        \n        if (sum == target) {\n            return {left, right};\n        } else if (sum < target) {\n            left++; // 和太小，左指针右移\n        } else {\n            right--; // 和太大，右指针左移\n        }\n    }\n    \n    return {}; // 没有找到\n}\n\n// 反转数组\nvoid reverseArray(std::vector<int>& nums) {\n    int left = 0;\n    int right = nums.size() - 1;\n    \n    while (left < right) {\n        // 交换左右指针指向的元素\n        std::swap(nums[left], nums[right]);\n        left++;\n        right--;\n    }\n}\n```\n\n### 3. 滑动窗口\n**应用场景**：最长无重复子串、最小覆盖子串、寻找所有字母异位词等\n```cpp\n#include <string>\n#include <unordered_map>\n\n// 最长无重复子串\nint lengthOfLongestSubstring(std::string s) {\n    int maxLength = 0;\n    int left = 0;\n    std::unordered_map<char, int> charMap;\n    \n    for (int right = 0; right < s.size(); right++) {\n        char c = s[right];\n        \n        // 如果字符已经在窗口中，移动左指针到重复字符的下一个位置\n        if (charMap.find(c) != charMap.end() && charMap[c] >= left) {\n            left = charMap[c] + 1;\n        }\n        \n        // 更新字符的位置\n        charMap[c] = right;\n        // 更新最大长度\n        maxLength = std::max(maxLength, right - left + 1);\n    }\n    \n    return maxLength;\n}\n```\n\n## 示例题目\n### 题目1：移除元素\n**描述**：给你一个数组 `nums` 和一个值 `val`，你需要原地移除所有数值等于 `val` 的元素，并返回移除后数组的新长度。\n**示例**：\n```\n输入: nums = [3,2,2,3], val = 3\n输出: 2, nums = [2,2]\n```\n**解法**：使用快慢指针\n```cpp\n#include <vector>\n\nint removeElement(std::vector<int>& nums, int val) {\n    int slow = 0;\n    \n    for (int fast = 0; fast < nums.size(); fast++) {\n        if (nums[fast] != val) {\n            nums[slow] = nums[fast];\n            slow++;\n        }\n    }\n    \n    return slow;\n}\n```\n\n### 题目2：盛最多水的容器\n**描述**：给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。找出其中的两条线，使得它们与 x 轴共同构成的容器可以容纳最多的水。\n**示例**：\n```\n输入: height = [1,8,6,2,5,4,8,3,7]\n输出: 49\n```\n**解法**：使用左右指针\n```cpp\n#include <vector>\n#include <algorithm>\n\nint maxArea(std::vector<int>& height) {\n    int left = 0;\n    int right = height.size() - 1;\n    int maxArea = 0;\n    \n    while (left < right) {\n        // 计算当前容器的面积\n        int currentArea = std::min(height[left], height[right]) * (right - left);\n        // 更新最大面积\n        maxArea = std::max(maxArea, currentArea);\n        \n        // 移动较短的边\n        if (height[left] < height[right]) {\n            left++;\n        } else {\n            right--;\n        }\n    }\n    \n    return maxArea;\n}\n```\n\n## 总结\n双指针是一种非常强大的算法技巧，它可以帮助我们高效地解决各种数组和链表相关的问题。通过合理地使用快慢指针、左右指针或滑动窗口，我们可以将时间复杂度从O(n²)降低到O(n)，大大提高算法的效率。\n掌握双指针的基本思想和常见应用场景，对于解决算法问题非常有帮助。在实际应用中，我们需要根据具体问题的特点，选择合适的双指针策略，并灵活调整指针的移动规则。";export{n as default};
