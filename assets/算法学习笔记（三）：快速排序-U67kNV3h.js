const n="---\ntitle: 算法学习笔记（三）：快速排序\ndate: 2023-01-03\ntag: 算法;快速排序\ntop: 0  \noriginal: 1\n---\n## 基本概念\n\n快速排序（Quick Sort）是一种高效的排序算法，它的核心思想是**分治策略**：\n\n1. **选择基准值**（pivot）：从数组中选择一个元素作为基准值\n2. **分区操作**（partition）：将数组分为两部分，小于基准值的元素放在左边，大于基准值的元素放在右边\n3. **递归排序**：对左右两个子数组分别进行快速排序\n\n快速排序的平均时间复杂度为O(n log n)，最坏情况下为O(n²)，但通过合理选择基准值可以避免最坏情况的发生。\n\n## 实现步骤\n\n1. **选择基准值**：通常选择数组的第一个元素、最后一个元素或中间元素作为基准值\n2. **分区操作**：\n   - 从数组两端开始遍历\n   - 左指针从左向右寻找大于基准值的元素\n   - 右指针从右向左寻找小于基准值的元素\n   - 交换找到的两个元素\n   - 重复上述过程，直到左指针超过右指针\n   - 将基准值与左指针指向的元素交换，完成分区\n3. **递归排序**：对基准值左右两侧的子数组分别进行快速排序\n\n## 实现代码\n\n### 基本实现\n\n```cpp\n#include <vector>\n\nstd::vector<int> quickSort(std::vector<int> nums) {\n    if (nums.size() <= 1) return nums;\n    \n    // 选择基准值\n    int pivot = nums[0];\n    std::vector<int> left, right;\n    \n    // 分区操作\n    for (size_t i = 1; i < nums.size(); i++) {\n        if (nums[i] < pivot) {\n            left.push_back(nums[i]);\n        } else {\n            right.push_back(nums[i]);\n        }\n    }\n    \n    // 递归排序并合并结果\n    left = quickSort(left);\n    right = quickSort(right);\n    \n    std::vector<int> result;\n    result.insert(result.end(), left.begin(), left.end());\n    result.push_back(pivot);\n    result.insert(result.end(), right.begin(), right.end());\n    \n    return result;\n}\n```\n\n### 原地实现（更高效）\n\n```cpp\n#include <vector>\n\nint partition(std::vector<int>& nums, int left, int right) {\n    // 选择最右边的元素作为基准值\n    int pivot = nums[right];\n    // 左指针\n    int i = left;\n    \n    // 遍历数组\n    for (int j = left; j < right; j++) {\n        // 如果当前元素小于基准值，交换元素并移动左指针\n        if (nums[j] < pivot) {\n            std::swap(nums[i], nums[j]);\n            i++;\n        }\n    }\n    \n    // 将基准值放到正确的位置\n    std::swap(nums[i], nums[right]);\n    return i; // 返回基准值的索引\n}\n\nvoid quickSortInPlace(std::vector<int>& nums, int left, int right) {\n    if (left < right) {\n        // 获取分区点\n        int pivotIndex = partition(nums, left, right);\n        // 递归排序左半部分\n        quickSortInPlace(nums, left, pivotIndex - 1);\n        // 递归排序右半部分\n        quickSortInPlace(nums, pivotIndex + 1, right);\n    }\n}\n\nvoid quickSort(std::vector<int>& nums) {\n    quickSortInPlace(nums, 0, nums.size() - 1);\n}\n```\n\n## 优化技巧\n\n1. **基准值选择优化**：\n   - **随机选择**：每次随机选择一个元素作为基准值\n   - **三数取中法**：选择数组的第一个、中间和最后一个元素的中位数作为基准值\n\n2. **小数组优化**：对于长度较小的子数组（如长度小于10），使用插入排序代替快速排序\n\n3. **尾递归优化**：减少递归调用的栈深度\n\n4. **重复元素优化**：对于包含大量重复元素的数组，可以使用三向切分（荷兰国旗问题）的方法\n\n### 三数取中法选择基准值\n\n```cpp\n#include <vector>\n\nint medianOfThree(std::vector<int>& nums, int left, int right) {\n    int mid = left + (right - left) / 2;\n    \n    // 对三个数进行排序\n    if (nums[left] > nums[mid]) {\n        std::swap(nums[left], nums[mid]);\n    }\n    if (nums[left] > nums[right]) {\n        std::swap(nums[left], nums[right]);\n    }\n    if (nums[mid] > nums[right]) {\n        std::swap(nums[mid], nums[right]);\n    }\n    \n    // 将中位数放到倒数第二个位置\n    std::swap(nums[mid], nums[right - 1]);\n    return right - 1; // 返回基准值的位置\n}\n\nint partitionOptimized(std::vector<int>& nums, int left, int right) {\n    // 使用三数取中法选择基准值\n    int pivotIndex = medianOfThree(nums, left, right);\n    int pivot = nums[pivotIndex];\n    \n    int i = left;\n    int j = right - 1;\n    \n    while (true) {\n        while (nums[++i] < pivot) {}\n        while (nums[--j] > pivot) {}\n        \n        if (i < j) {\n            std::swap(nums[i], nums[j]);\n        } else {\n            break;\n        }\n    }\n    \n    std::swap(nums[i], nums[right - 1]);\n    return i;\n}\n\nvoid quickSortOptimized(std::vector<int>& nums, int left, int right) {\n    if (left < right) {\n        // 小数组优化：使用插入排序\n        if (right - left <= 10) {\n            // 插入排序实现\n            for (int i = left + 1; i <= right; i++) {\n                int key = nums[i];\n                int j = i - 1;\n                while (j >= left && nums[j] > key) {\n                    nums[j + 1] = nums[j];\n                    j--;\n                }\n                nums[j + 1] = key;\n            }\n            return;\n        }\n        \n        int pivotIndex = partitionOptimized(nums, left, right);\n        quickSortOptimized(nums, left, pivotIndex - 1);\n        quickSortOptimized(nums, pivotIndex + 1, right);\n    }\n}\n```\n\n### 三向切分（处理重复元素）\n\n```cpp\n#include <vector>\n\nvoid quickSort3Way(std::vector<int>& nums, int left, int right) {\n    if (left >= right) return;\n    \n    // 选择基准值\n    int pivot = nums[left];\n    int lt = left;     // 小于基准值的区域边界\n    int i = left + 1;   // 当前遍历的位置\n    int gt = right;     // 大于基准值的区域边界\n    \n    while (i <= gt) {\n        if (nums[i] < pivot) {\n            // 当前元素小于基准值，交换到左边\n            std::swap(nums[lt], nums[i]);\n            lt++;\n            i++;\n        } else if (nums[i] > pivot) {\n            // 当前元素大于基准值，交换到右边\n            std::swap(nums[i], nums[gt]);\n            gt--;\n            // 注意这里i不递增，因为交换过来的元素还没有比较\n        } else {\n            // 当前元素等于基准值，继续向右遍历\n            i++;\n        }\n    }\n    \n    // 递归排序左右两部分\n    quickSort3Way(nums, left, lt - 1);\n    quickSort3Way(nums, gt + 1, right);\n}\n\nvoid quickSort3Way(std::vector<int>& nums) {\n    quickSort3Way(nums, 0, nums.size() - 1);\n}\n```\n\n## 时间复杂度分析\n\n- **平均时间复杂度**：O(n log n)\n- **最坏时间复杂度**：O(n²)（当数组已经有序或所有元素都相同时）\n- **最好时间复杂度**：O(n log n)\n- **空间复杂度**：\n  - 基本实现：O(n)（需要额外的数组空间）\n  - 原地实现：O(log n)（递归调用栈的深度）\n\n## 优缺点\n\n### 优点\n\n1. **平均时间复杂度低**：O(n log n)，比冒泡排序、插入排序等算法高效\n2. **原地排序**：不需要额外的存储空间\n3. **适用于大规模数据**：在处理大规模数据时表现良好\n4. **缓存友好**：由于是原地操作，对缓存利用较好\n\n### 缺点\n\n1. **最坏时间复杂度高**：O(n²)，但可以通过合理选择基准值避免\n2. **不稳定**：相同元素的相对位置可能会改变\n3. **递归实现**：对于非常大的数组可能会导致栈溢出（可以通过迭代实现解决）\n\n## 应用场景\n\n1. **大规模数据排序**：快速排序是处理大规模数据的首选算法之一\n2. **需要原地排序的场景**：当内存空间有限时\n3. **作为其他算法的子程序**：例如，在快速选择算法中使用\n\n## 与其他排序算法的比较\n\n| 排序算法 | 平均时间复杂度 | 最坏时间复杂度 | 空间复杂度 | 稳定性 |\n|---------|--------------|--------------|-----------|--------|\n| 快速排序 | O(n log n)   | O(n²)        | O(log n)  | 不稳定 |\n| 归并排序 | O(n log n)   | O(n log n)   | O(n)      | 稳定   |\n| 堆排序   | O(n log n)   | O(n log n)   | O(1)      | 不稳定 |\n| 插入排序 | O(n²)        | O(n²)        | O(1)      | 稳定   |\n| 冒泡排序 | O(n²)        | O(n²)        | O(1)      | 稳定   |\n\n## 总结\n\n快速排序是一种高效的排序算法，通过分治策略和分区操作实现。它的核心思想是选择一个基准值，将数组分为两部分，然后递归排序这两部分。\n\n虽然快速排序在最坏情况下的时间复杂度为O(n²)，但通过合理选择基准值（如三数取中法）和使用优化技巧（如三向切分），可以避免最坏情况的发生，并在大多数实际场景中表现出优异的性能。\n\n快速排序是一种原地排序算法，不需要额外的存储空间，适用于处理大规模数据。它是许多编程语言标准库中排序函数的实现选择，也是算法学习中的重要内容。";export{n as default};
