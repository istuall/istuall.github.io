const n="---\ntitle: 算法学习笔记（一）：二分查找\ndate: 2023-01-01\ntag: 算法;二分查找\ntop: 0\noriginal: 1\n---\n## 基本概念\n\n二分查找（Binary Search）是一种在**有序数组**中查找特定元素的高效算法。它的基本思想是：\n\n1. 每次将查找范围缩小一半\n2. 比较中间元素与目标值的大小\n3. 根据比较结果决定在左半部分还是右半部分继续查找\n\n二分查找的时间复杂度为O(log n)，比线性查找的O(n)要高效得多，特别是在处理大规模数据时。\n\n## 实现条件\n\n使用二分查找的前提条件：\n\n1. **数组必须是有序的**（升序或降序）\n2. **数组中的元素必须是可比较的**\n3. **数组的长度是有限的**\n\n## 实现模板\n\n### 基本模板（查找目标值）\n\n```cpp\n#include <vector>\n\nint binarySearch(std::vector<int>& nums, int target) {\n    int left = 0;\n    int right = nums.size() - 1;\n    \n    while (left <= right) {\n        // 计算中间索引，避免整数溢出\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] == target) {\n            return mid; // 找到目标值，返回索引\n        } else if (nums[mid] < target) {\n            left = mid + 1; // 目标值在右半部分\n        } else {\n            right = mid - 1; // 目标值在左半部分\n        }\n    }\n    \n    return -1; // 未找到目标值\n}\n```\n\n### 查找左边界（第一个大于等于目标值的位置）\n\n```cpp\n#include <vector>\n\nint findLeftBound(std::vector<int>& nums, int target) {\n    int left = 0;\n    int right = nums.size() - 1;\n    int result = -1;\n    \n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] >= target) {\n            result = mid;\n            right = mid - 1; // 继续向左查找\n        } else {\n            left = mid + 1;\n        }\n    }\n    \n    return result;\n}\n```\n\n### 查找右边界（最后一个小于等于目标值的位置）\n\n```cpp\n#include <vector>\n\nint findRightBound(std::vector<int>& nums, int target) {\n    int left = 0;\n    int right = nums.size() - 1;\n    int result = -1;\n    \n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] <= target) {\n            result = mid;\n            left = mid + 1; // 继续向右查找\n        } else {\n            right = mid - 1;\n        }\n    }\n    \n    return result;\n}\n```\n\n## 常见应用场景\n\n### 1. 在有序数组中查找元素\n\n这是二分查找最基本的应用场景，直接使用基本模板即可。\n\n### 2. 查找插入位置\n\n**问题**：给定一个有序数组和一个目标值，在数组中找到目标值，并返回其索引。如果目标值不存在于数组中，返回它将会被按顺序插入的位置。\n\n**解法**：使用查找左边界的模板\n\n```cpp\n#include <vector>\n\nint searchInsert(std::vector<int>& nums, int target) {\n    int left = 0;\n    int right = nums.size() - 1;\n    \n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] == target) {\n            return mid;\n        } else if (nums[mid] < target) {\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    \n    return left; // 未找到，返回插入位置\n}\n```\n\n### 3. 寻找峰值\n\n**问题**：峰值元素是指其值大于左右相邻值的元素。给定一个输入数组 nums，其中 nums[i] ≠ nums[i+1]，找到峰值元素并返回其索引。\n\n**解法**：利用二分查找的思想，根据中间元素与下一个元素的大小关系判断峰值的位置\n\n```cpp\n#include <vector>\n\nint findPeakElement(std::vector<int>& nums) {\n    int left = 0;\n    int right = nums.size() - 1;\n    \n    while (left < right) {\n        int mid = left + (right - left) / 2;\n        \n        if (nums[mid] < nums[mid + 1]) {\n            left = mid + 1; // 峰值在右半部分\n        } else {\n            right = mid; // 峰值在左半部分\n        }\n    }\n    \n    return left; // 左指针最终指向峰值\n}\n```\n\n### 4. 平方根计算\n\n**问题**：实现 int sqrt(int x) 函数。计算并返回 x 的平方根，其中 x 是非负整数。\n\n**解法**：使用二分查找在[0, x]范围内查找平方根\n\n```cpp\nint mySqrt(int x) {\n    if (x == 0) return 0;\n    \n    int left = 1;\n    int right = x;\n    int result = 0;\n    \n    while (left <= right) {\n        int mid = left + (right - left) / 2;\n        \n        // 避免整数溢出\n        if (mid == x / mid) {\n            return mid;\n        } else if (mid < x / mid) {\n            result = mid; // 可能的结果\n            left = mid + 1;\n        } else {\n            right = mid - 1;\n        }\n    }\n    \n    return result;\n}\n```\n\n## 注意事项\n\n1. **整数溢出问题**：计算中间索引时，使用 `left + (right - left) / 2` 而不是 `(left + right) / 2`，可以避免整数溢出\n\n2. **循环条件**：根据具体问题选择使用 `left <= right` 还是 `left < right`\n\n3. **边界处理**：注意数组的边界情况，如空数组、目标值在数组范围外等\n\n4. **顺序问题**：确保数组是有序的，否则需要先排序\n\n## 时间复杂度分析\n\n- **时间复杂度**：O(log n)，其中 n 是数组的长度。每次查找都会将范围缩小一半，因此最多需要 \\( \\log_2 n \\) 次比较\n\n- **空间复杂度**：O(1)，只需要常数级的额外空间\n\n## 数学原理\n\n二分查找的核心思想基于数学中的**分治策略**，其时间复杂度的推导如下：\n\n$$\nT(n) = T(n/2) + O(1)\n$$\n\n通过主定理（Master Theorem）可以解得：\n\n$$\nT(n) = O(\\log n)\n$$\n\n### 中间索引计算\n\n为了避免整数溢出，我们使用以下公式计算中间索引：\n\n$$\nmid = left + \\frac{right - left}{2}\n$$\n\n而不是简单的：\n\n$$\nmid = \\frac{left + right}{2}\n$$\n\n### 查找过程的数学描述\n\n二分查找的过程可以看作是在区间 \\([left, right]\\) 中不断缩小查找范围的过程，每次比较后：\n\n1. 如果 \\( nums[mid] = target \\)，则找到目标值\n2. 如果 \\( nums[mid] < target \\)，则在 \\([mid+1, right]\\) 中继续查找\n3. 如果 \\( nums[mid] > target \\)，则在 \\([left, mid-1]\\) 中继续查找\n\n## 总结\n\n二分查找是一种非常高效的查找算法，适用于有序数组的查找问题。它的核心思想是通过不断缩小查找范围来快速定位目标值。掌握二分查找的基本原理和实现模板，对于解决各种查找相关的算法问题非常有帮助。\n\n在实际应用中，我们需要根据具体问题的特点，灵活运用二分查找的思想，选择合适的实现方式。";export{n as default};
