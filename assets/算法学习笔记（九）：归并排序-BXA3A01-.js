const n="---\ntitle: 算法学习笔记（九）：归并排序\ndate: 2023-01-09\ntag: 算法;归并排序\ntop: 0  \noriginal: 1\n---\n## 1. 算法概念\n归并排序（Merge Sort）是一种基于分治思想的排序算法，它将待排序序列分成若干子序列，将每个子序列排序，然后将排序好的子序列合并成一个有序序列。\n归并排序的基本思想是：\n1. **分解**：将待排序序列分成两个长度大致相等的子序列\n2. **解决**：递归地对两个子序列进行归并排序\n3. **合并**：将两个已排序的子序列合并成一个有序序列\n\n## 2. 算法实现\n### 2.1 核心函数\n```javascript\n// 合并两个已排序的子数组\nfunction merge(left, right) {\n  const result = [];\n  let i = 0, j = 0;\n  \n  // 比较两个子数组的元素，按顺序放入结果数组\n  while (i < left.length && j < right.length) {\n    if (left[i] <= right[j]) {\n      result.push(left[i]);\n      i++;\n    } else {\n      result.push(right[j]);\n      j++;\n    }\n  }\n  \n  // 将剩余元素添加到结果数组\n  while (i < left.length) {\n    result.push(left[i]);\n    i++;\n  }\n  \n  while (j < right.length) {\n    result.push(right[j]);\n    j++;\n  }\n  \n  return result;\n}\n\n// 归并排序主函数\nfunction mergeSort(arr) {\n  // 基本情况：数组长度为1或0时，已经有序\n  if (arr.length <= 1) {\n    return arr;\n  }\n  \n  // 计算中间位置\n  const mid = Math.floor(arr.length / 2);\n  \n  // 递归地对左半部分和右半部分进行排序\n  const left = mergeSort(arr.slice(0, mid));\n  const right = mergeSort(arr.slice(mid));\n  \n  // 合并两个已排序的子数组\n  return merge(left, right);\n}\n```\n\n### 2.2 使用示例\n```javascript\n// 测试归并排序\nconst arr = [12, 11, 13, 5, 6, 7];\nconsole.log('原始数组:', arr);\nconst sortedArr = mergeSort(arr);\nconsole.log('排序后数组:', sortedArr);\n\n// 输出结果:\n// 原始数组: [12, 11, 13, 5, 6, 7]\n// 排序后数组: [5, 6, 7, 11, 12, 13]\n```\n\n## 3. 算法分析\n### 3.1 时间复杂度\n- **最好情况**：O(n log n)\n- **最坏情况**：O(n log n)\n- **平均情况**：O(n log n)\n\n### 3.2 空间复杂度\n- **空间复杂度**：O(n)，需要额外的空间来存储合并过程中的临时数组\n\n### 3.3 算法特点\n- **稳定性**：稳定排序\n- **适用场景**：适用于大规模数据的排序，特别是外部排序（数据量超过内存容量）\n- **优势**：时间复杂度稳定，为O(n log n)；是稳定排序算法\n- **劣势**：空间复杂度较高，为O(n)；对于小规模数据，性能不如插入排序等简单排序算法\n\n## 4. 代码优化\n### 4.1 优化点\n1. **减少数组创建**：使用辅助数组，避免每次递归都创建新数组\n2. **优化小规模数据处理**：对于小规模子数组，使用插入排序\n3. **避免不必要的复制**：在合并时，直接操作原数组\n\n### 4.2 优化实现\n```javascript\n// 优化版本：使用辅助数组，避免多次创建新数组\nfunction optimizedMergeSort(arr) {\n  // 创建辅助数组\n  const aux = new Array(arr.length);\n  \n  // 内部排序函数\n  function sort(low, high) {\n    // 基本情况\n    if (high <= low) return;\n    \n    // 优化：对于小规模子数组，使用插入排序\n    if (high - low <= 10) {\n      insertionSort(arr, low, high);\n      return;\n    }\n    \n    const mid = low + Math.floor((high - low) / 2);\n    sort(low, mid);\n    sort(mid + 1, high);\n    \n    // 优化：如果已经有序，跳过合并步骤\n    if (arr[mid] <= arr[mid + 1]) return;\n    \n    merge(arr, aux, low, mid, high);\n  }\n  \n  // 插入排序函数\n  function insertionSort(arr, low, high) {\n    for (let i = low + 1; i <= high; i++) {\n      const key = arr[i];\n      let j = i - 1;\n      while (j >= low && arr[j] > key) {\n        arr[j + 1] = arr[j];\n        j--;\n      }\n      arr[j + 1] = key;\n    }\n  }\n  \n  // 合并函数\n  function merge(arr, aux, low, mid, high) {\n    // 复制到辅助数组\n    for (let k = low; k <= high; k++) {\n      aux[k] = arr[k];\n    }\n    \n    let i = low, j = mid + 1;\n    for (let k = low; k <= high; k++) {\n      if (i > mid) {\n        arr[k] = aux[j++];\n      } else if (j > high) {\n        arr[k] = aux[i++];\n      } else if (aux[j] < aux[i]) {\n        arr[k] = aux[j++];\n      } else {\n        arr[k] = aux[i++];\n      }\n    }\n  }\n  \n  sort(0, arr.length - 1);\n  return arr;\n}\n```\n\n## 5. 应用场景\n1. **大规模数据排序**：归并排序的时间复杂度稳定，适用于大规模数据的排序\n2. **外部排序**：当数据量超过内存容量时，可以使用归并排序的思想进行外部排序\n3. **链表排序**：归并排序特别适合链表排序，因为它不需要随机访问元素\n4. **求逆序对**：可以通过归并排序的过程计算数组中的逆序对数量\n\n## 6. 总结\n归并排序是一种高效、稳定的排序算法，它基于分治思想，将待排序序列分成子序列，分别排序后再合并。归并排序的时间复杂度为O(n log n)，空间复杂度为O(n)。\n归并排序的核心思想是：\n1. 分解：将待排序序列分成两个子序列\n2. 解决：递归地对两个子序列进行排序\n3. 合并：将两个已排序的子序列合并成一个有序序列\n归并排序的优点是时间复杂度稳定，是稳定排序算法，特别适合处理大规模数据和外部排序。缺点是空间复杂度较高，需要额外的空间来存储临时数组。\n通过理解归并排序的原理和实现，我们可以更好地掌握分治思想在算法设计中的应用，以及在实际问题中选择合适的排序算法。";export{n as default};
